#For use with Malta
CLFS=/opt/aclfs.MIPS.little
CLFS_ARCH=mips
CLFS_ABI=32
CLFS_HOST=$(echo ${MACHTYPE} | sed "s/-[^-]*/-cross/")
CLFS_TARGET=mipsel-unknown-linux-uclibc
CLFS_MIPS_LEVEL=1
CLFS_FLOAT=soft
CLFS_ENDIAN=little
kConfig=$PWD/configs/kernel/kernel.mips.little
PATH=${CLFS}/cross-tools/bin:/bin:/usr/bin

