#For use with i386
CLFS=/opt/aCLFS.I386.little
CLFS_ARCH=i386
CLFS_HOST=$(echo ${MACHTYPE} | sed "s/-[^-]*/-cross/")
CLFS_TARGET=i486-pc-linux-uclibc
CLFS_CPU=i486
CLFS_ENDIAN=little
kConfig=$PWD/configs/kernel/kernel.i386.little
PATH=${CLFS}/cross-tools/bin:/bin:/usr/bin

